import 'package:flutter/material.dart';


class paquetes  extends StatelessWidget  {
  const paquetes(value, {Key? key}) : super(key: key);

  get value => null;

  @override
  Widget build(BuildContext context) {
    return   Center(
        child: Container(
           child: Text('value'),
        ),

    );
  }
}